FROM nginx:alpine

# Copiar los archivos HTML al directorio web de nginx
COPY web-app/ /usr/share/nginx/html/

# Copiar configuración personalizada de nginx para API proxy
COPY nginx.conf /etc/nginx/nginx.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]